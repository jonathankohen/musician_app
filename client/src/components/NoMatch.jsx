import React from "react";

export default function NoMatch() {
	return (
		<header>
			<h1>No Match.</h1>
			<p>
				Sorry, this path doesn't match any routes in our current router.
			</p>
		</header>
	);
}
